# Changelog

## [0.0.5] - 2026-04-04

### Fixed
- 修复模块名包含 `-` 时解析错误的问题
- 支持新格式 `:` 分隔符（`module:env`），解决模块名/环境名中 `-` 的歧义
- 保持向后兼容，旧格式 `-` 分隔符仍可正常使用

### Changed
- `parseMode` 优先使用 `:` 匹配新格式
- `parseModeLegacy` 兼容老版本 `-` 分隔符
- 模块名支持包含 `-`
- 环境名支持包含 `-`

## [0.0.4] - 2026-02-10

### Added
- 初始版本发布
